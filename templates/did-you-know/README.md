# Did You Know template

MF-001's single template maps the fixture's `intro`, `content`, and `outro` fields to four timed phases in `godot/main.gd`. Visual timing and behavior belong to the template; wording and output format belong to the fixture.
