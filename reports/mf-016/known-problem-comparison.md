# Known-problem comparison

## MF-015R2 problem state

The frozen comparison frame contains a thick lower-left foreground line crossing toward the reactor, a pale structural/rail system traversing the composition, and an additional angled instrument bank packed into the remaining space. These elements add density but weaken hierarchy and create avoidable crossings.

## MF-016 corrected state

- Deletes the crossing foreground pipe entirely.
- Deletes the pale cross-scene rail and vertical-member system.
- Uses one rectangular analog bank aligned to the left support zone.
- Protects the reactor with overlapping hero/no-occlusion zones.
- Keeps the upper-left region intentionally empty and dark.
- Uses only vertical peripheral architecture and a shallow bottom plinth for depth.

The direct artifact is `artifacts/mf-016/before-after/problem-vs-corrected.png`. The comparison demonstrates deletion and hierarchy—not increased object count—as the repair strategy.
