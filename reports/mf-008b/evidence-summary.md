# MF-008B Evidence Summary

Result: **NEEDS_ENGINEERING**. The directed batch stopped at creative capability preflight. All three jobs reached the terminal `NEEDS_ENGINEERING` state; zero renders were attempted, zero candidates reached `READY_FOR_REVIEW`, and nothing was published.

The three detailed briefs pass completeness checks and preserve one Unknown Process campaign identity. The frozen `unknown_process_recovered_record_v1` grammar and all 14 pinned inputs remain byte-exact to grammar origin `74ae32bd32f5480007ab01feaa77e83625a9a11f`. Execution occurred at Git ref `b4aded0b4d2498c6964e096724ae9767ee760aa6`. Renderer changes: **0**.

The frozen stage always advances through its tracking, Leo/Zeph bridge, and biometric record branches. It provides no configuration to select only one mechanism per job, no job-specific investigation events, and no controls for the requested per-video palette, projection, camera, pulse, or CTA profiles. The approved cue map contains only `baseline_full`, so assigning three purpose-specific sections would require unapproved offsets. Its pinned runtime is 28 seconds.

Because the core distinction cannot be executed, running the existing renderer would reproduce the MF-008 defect: three text variants containing the same three mechanisms. Fail-closed policy therefore prohibited rendering and downstream technical media validation. Contact-sheet, motion-evidence, representative-frame, artifact-location, and artifact-hash fields are intentionally absent rather than misleading.

Human editorial/release status: **BLOCKED — NO CANDIDATES**. Creative distinctness: **NOT DEMONSTRATED**. Do not scale to a ten-video campaign yet.

The next scoped engineering slice needs human authorization because MF-008B explicitly prohibits renderer changes. It should expose validated content-fixture controls for mechanism selection, event names/timing, bounded cosmetic profiles, and camera/pulse behavior. Separately, a human must approve cue-map sections for pursuit, investigation, and revelation before audio variation can pass.
