# MF-004 Evidence Summary

Technical acceptance is produced by `./scripts/mf-004-acceptance.sh`. The evidence package contains materialized and executed timelines, independent per-fixture validation, one midpoint frame for every beat, finalized MP4s, production metrics, and a combined contact sheet.

All three subjects use `godot/mf002.gd`, `godot/beat_timeline.gd`, the same visual grammar, MF-002R1 text fitting, and the MF-003 media slot. Subject-specific renderer code is zero. Human pacing/creative review remains required and is recorded as `PENDING` in `result.json`.
